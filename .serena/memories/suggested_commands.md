# Suggested Commands

```bash
# Build (debug)
cargo build

# Build (release)
cargo build --release

# Run binary
cargo run

# Run tests
cargo test

# Check without producing artefacts (fast)
cargo check

# Lint
cargo clippy

# Format
cargo fmt
```
