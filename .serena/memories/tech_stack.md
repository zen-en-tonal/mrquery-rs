# Tech Stack

- **Language**: Rust (edition 2024)
- **Crate type**: binary (`src/main.rs`)
- **Package manager / build tool**: Cargo
- **Dependencies**: none declared in `Cargo.toml` yet
- **Rust toolchain**: not pinned (no `rust-toolchain.toml`); project requires edition 2024 support (Rust ≥ 1.85)
- **OS target**: Linux (development host)
