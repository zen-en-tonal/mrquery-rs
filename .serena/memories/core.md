# mrquery-rs — Core

Single-binary Rust crate. Early-stage / scaffold only as of 2026-06-16.

## Source layout

- `src/main.rs` — sole source file; currently hello-world placeholder
- `Cargo.toml` — package manifest (edition 2024, no deps yet)
- `docs/` — empty; reserved for future documentation
- `target/` — build artefacts (gitignored)

## Key invariants

- One binary target (`mrquery-rs`), no lib crate, no workspace.
- No external dependencies declared yet.

## Related memories

- Tech stack details: `mem:tech_stack`
- Build/dev commands: `mem:suggested_commands`
- Task-completion checklist: `mem:task_completion`
- Code conventions: `mem:conventions`
